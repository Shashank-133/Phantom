# PHANTOM backend dependencies
# Python 3.11
# NOTE: On Windows/Linux without GPU, install torch CPU-only first:
#   pip install torch==2.2.1 --index-url https://download.pytorch.org/whl/cpu
# then: pip install -r requirements.txt

# --- Web ---
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9
websockets==12.0

# --- Settings / validation ---
pydantic==2.6.3
pydantic-settings==2.2.1
python-dotenv==1.0.1

# --- Databases ---
sqlalchemy==2.0.27
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1
neo4j==5.18.0

# --- Task queue ---
celery==5.3.6
redis==5.0.2
flower==2.0.1

# --- PDF parsing & generation ---
pymupdf==1.23.26
pikepdf==8.13.0
reportlab==4.1.0
imagehash==4.3.1
Pillow==10.2.0

# --- Cryptography ---
cryptography==42.0.5

# --- ML ---
# torch installed separately (CPU-only) — see header note
transformers==4.38.2
sentence-transformers==2.5.1
scikit-learn==1.4.1.post1
scipy==1.12.0
numpy==1.26.4

# --- Graph ---
networkx==3.2.1
python-louvain==0.16

# --- Vector search ---
faiss-cpu==1.7.4

# --- Fuzzy text matching (for name similarity, Upgrade A) ---
rapidfuzz==3.6.2

# --- Optional LLM narrative (Phase 2) ---
google-generativeai==0.4.1

# --- Logging & utilities ---
loguru==0.7.2
httpx==0.27.0
orjson==3.9.15

# --- Tests ---
pytest==8.0.2
pytest-asyncio==0.23.5
