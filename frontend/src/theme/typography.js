// Typography tokens. Most components should reach for Tailwind utility classes
// (`font-serif italic`, `font-mono`, etc.) — this file is for places that need
// raw string values (e.g. canvas/SVG text or programmatic style attributes).
export const fonts = {
  sans: '"Inter", ui-sans-serif, system-ui, sans-serif',
  serif: '"Instrument Serif", "Cormorant Garamond", ui-serif, Georgia, serif',
  mono: '"JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace',
};
